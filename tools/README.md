# tools — Helper scripts

- `trajectory_plotter.py` — plot estimated vs ground-truth trajectories
- `benchmark_runner.sh` — run benchmark suite end-to-end
- `data_recorder.py` — record sensor streams (post-Phase 5)
