# embedded — Phase 5 (stub)

STM32 firmware for the sensor node (IMU + GPS + encoders).

- Bare-metal + HAL combination
- FreeRTOS multitasking
- UART/CAN communication with the host

See the Phase 5 plan in `docs/roadmap.md`.
