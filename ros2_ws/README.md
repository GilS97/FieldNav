# ros2_ws — Phase 4 (stub)

This will host the ROS 2 colcon workspace with packages:

- `fieldnav_msgs` — custom messages
- `fieldnav_localization_node` — wraps fieldnav::filters
- `fieldnav_sensor_bridge` — bridge between embedded layer and ROS 2
- `fieldnav_bringup` — launch files and configuration

See the Phase 4 plan in `docs/roadmap.md`.
