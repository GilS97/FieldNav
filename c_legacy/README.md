# c_legacy — Phase 1 educational matrix library

A minimal dynamic matrix library in C, used to practice:

- pointer arithmetic
- manual memory management
- dimension validation
- compilation with sanitizers (ASan, UBSan)
- writing tests with bare-bones C assertions

This is **not** the production code. Production C++ uses Eigen (see `core/`).
