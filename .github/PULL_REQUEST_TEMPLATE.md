## Summary

<!-- One sentence: what changes and why. -->

## Context

<!-- What is the problem this PR solves? What approach was chosen and why? -->

## Changes

- [ ] Code change 1
- [ ] Code change 2

## Validation

- [ ] `cmake --preset debug && cmake --build build/debug` passes
- [ ] `ctest --test-dir build/debug --output-on-failure` passes
- [ ] `cmake --preset asan && cmake --build build/asan && ctest --test-dir build/asan` passes
- [ ] `clang-format --dry-run -Werror` passes
- [ ] Documentation updated if applicable

## Related

<!-- ADR-XXXX or issue references. -->
